services:
  - type: web
    name: Fitness Equation
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "./bin/render-start.sh"
    envVars:
      - key: "DATABASE_URL"
        value: <% DATABASE_URL %>

databases:
  - name: brian
    plan: free
    ipAllowList: []
