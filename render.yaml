services:
  - type: web
    name: Fitness Equation
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "./bin/render-start.sh"
    envVars:
    - key: "DATABASE_URL"
      value: "postgresql://brian:199613@localhost:5432/db"

databases:
  - name: brian
    plan: free
    ipAllowList: []
